import React from 'react';

export const ResultsList = (props) => (
    <div>
        
        Results List Component
    </div>
);

export default ResultsList;

// retrieves 3 highest scoring results
// renders each one individaully

// Refer to ExpenseList.js & ExpenseListItem.js
